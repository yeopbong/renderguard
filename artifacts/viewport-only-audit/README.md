# Historical viewport-only results

These are the original fixed-viewport experiment records. Supplying Playwright `clip` without `fullPage` cut off scroll height. The corrected corpus was rerendered with the same splits and training recipe after these results had been read. These records belong to the superseded model; corrected results are in [`artifacts/model-results.json`](../model-results.json).
